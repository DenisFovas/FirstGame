keydown diferit de keypressed

de folosit mai des: onkeydown (w3 school/ resurse)
					onkeyup (w3 school/ resurse)

in Firefox nu merge onkeyup/down

trebuie delcarat intr-o functie, ca si parametru functia "event", atat la apelul din codul html cat si la functie Js

"event.which || event.keyCode" sunt pentru a merge pe browsere

codurile sunt puse in ordinea literelor(ordine alfababetica)(a, b, c, d, e etc.)

	<-- COD TASTE -->
function keyCode(event) {
    var x = event.keyCode;
    if (event.keyCode == 27) {
        alert ("You pressed the Escape key!");
    }
}

	<-- cod taste -->
function TastaApasata(event){
	var tastaAleasa = event.which || event.keyCode;
	if (tastaAleasa === "a")
		alert ("You pressed the Escape key!");	else if (tastaAleasa === "s")
		
	else if(tasta aleasa === "d")
		alert ("You pressed the Escape key!");
	else if(tasta aleasa === "w")
		alert ("You pressed the Escape key!");
	else if (minigameMonede.activare == true){
		alert ("You pressed the Escape key!");
	} 
}